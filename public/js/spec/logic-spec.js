// it ("returns true if you can make the good given the supplies", function() {})

describe("when supplies are still greater than 0 for all ingredients", function(){
  it("reduces supplies when make is called ", function() {
    bakery['co'];
    make()
    expect()
  });
});

describe("when supplies are out for a particular ingredients", function(){
  it("does not reduce supplies when make is called ", function() {

  });
});